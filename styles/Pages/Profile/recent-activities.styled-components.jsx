/* eslint-disable import/prefer-default-export */
import styled from 'styled-components';

import {
  letterColor,
} from '../../Colors/color-scheme.styled-components';

export const RecentActivitiesH2 = styled.div`
  font-size: 20px;
  color: ${letterColor};
  font-weight: 900;
  margin: 10px auto;
  display: table;
`;
